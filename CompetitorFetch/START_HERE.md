# 🚀 新手入门指南

> 5分钟快速上手用户反馈分析系统

## ⚡ 最快开始方式

### Windows用户

**双击运行 `启动.bat`** → 选择操作

```
[1] 安装依赖（首次使用）
[2] 运行反馈分析演示
[3] 运行请求分析演示
[4] 分析文件（反馈内容）
[5] 分析文件（请求内容）
[6] 打包为EXE
```

### 其他系统

```bash
# 安装依赖
pip install -r requirements.txt

# 运行演示
python src/main.py demo_data.xlsx

# 分析自己的数据
python src/main.py your_feedback.xlsx
```

---

## 📝 准备数据（4种方式）

### 方式1：使用反馈模板 ⭐

1. 打开 `feedback_template.xlsx`
2. 删除示例数据
3. 填写用户评价/意见
4. 保存并运行分析

```bash
python src/main.py feedback_template.xlsx --type feedback
```

### 方式2：使用请求模板 ⭐ 新功能

1. 打开 `request_template.xlsx`
2. 删除示例数据
3. 填写功能需求/建议
4. 保存并运行分析

```bash
python src/main.py request_template.xlsx --type request
```

### 方式3：自己的Excel文件

**反馈数据**：
| 反馈内容 |
|----------|
| 软件太卡顿了 |
| 界面很美观 |

**请求数据**：
| 请求内容 |
|----------|
| 希望增加导出功能 |
| 建议优化性能 |

**自动识别的列名**：
- 反馈、feedback、评论、comment
- 请求、request、需求、requirement
- 内容、content、意见、建议

### 方式4：手动指定

```bash
# 指定列名
python src/main.py data.xlsx --type request --column 你的列名

# 指定输出目录
python src/main.py data.xlsx --type feedback --output 我的报告
```

---

## 📊 查看结果

运行后打开 `output` 目录，会看到**以文件名命名的分析文件夹**：

### 反馈分析结果
```
output/
└── 文件名_时间戳/
    ├── README.txt          ← 先看这个！快速摘要
    ├── 分析报告.xlsx        ← 详细Excel报告（3个Sheet）
    ├── 情感分布图.png       ← 正面/负面/中性占比
    ├── 痛点词云图.png       ← 高频问题词云
    └── 痛点排行图.png       ← Top 15问题柱状图
```

### 请求分析结果 ⭐
```
output/
└── 文件名_时间戳/
    ├── README.txt          ← 先看这个！快速摘要
    ├── 请求分析报告.xlsx    ← 详细Excel报告（3个Sheet）
    ├── 请求类型分布图.png   ← 8种类型占比
    ├── 紧急度分布图.png     ← 高/中紧急度占比
    ├── 功能需求词云图.png   ← 高频需求词云
    └── 功能需求排行图.png   ← Top 15需求柱状图
```

**例如**：分析 `my_feedback.csv` 会生成 `my_feedback_20251201_143000/`

**推荐顺序**：
1. 📖 先打开 `README.txt` 快速了解结果
2. 📊 打开Excel报告查看详细数据（包含3个Sheet）
3. 🎨 查看图表进行可视化分析

---

## 🎯 常用命令

```bash
# 反馈分析（默认）
python src/main.py feedback.xlsx

# 请求分析 ⭐ 新功能
python src/main.py requests.xlsx --type request

# 指定列名
python src/main.py your_file.xlsx --column 用户评论

# 指定输出目录
python src/main.py your_file.xlsx --output 报告文件夹

# 完整命令
python src/main.py data.xlsx --type request --column 请求内容 --output reports
```

### 📝 分析类型说明

| 参数 | 说明 | 适用场景 |
|------|------|----------|
| `--type feedback` | 反馈分析（默认） | 用户评价、满意度调查 |
| `--type request` | 请求分析 | 功能需求、改进建议 |

---

## ⚙️ 自定义配置

### 修改配置文件

编辑 `config/config.json`：

```json
{
  "output_dir": "output",        // 输出目录
  "top_pain_points": 20,         // 提取多少个痛点
  "sentiment_thresholds": {
    "positive_min": 0.6,         // 正面情感阈值
    "neutral_min": 0.4           // 中性情感阈值
  }
}
```

### 添加自定义词汇

编辑 `config/custom_dict.txt`，添加行业词汇：

```
产品名 10000 n
功能名 10000 n
```

---

## 💡 实用技巧

### 技巧1：批量分析

将多个Excel文件放在一个文件夹，批量分析：

```bash
# Windows
for %f in (data\*.xlsx) do python src/main.py "%f"

# Linux/Mac
for file in data/*.xlsx; do python src/main.py "$file"; done
```

### 技巧2：在Python代码中使用

```python
from src.main import FeedbackAnalysisSystem

# 反馈分析
feedback_system = FeedbackAnalysisSystem(analysis_type='feedback')
result = feedback_system.analyze_from_excel('feedback.xlsx')
print(result['summary'])      # 情感统计
print(result['pain_points'])  # 痛点列表

# 请求分析
request_system = FeedbackAnalysisSystem(analysis_type='request')
result = request_system.analyze_from_excel('requests.xlsx')
print(result['summary'])      # 请求统计
print(result['features'])     # 高频需求
```

### 技巧3：定期分析

创建定时任务，每周自动分析最新反馈：

```bash
# Windows: 任务计划程序
# Linux: crontab
0 9 * * 1 python /path/to/src/main.py /path/to/weekly_feedback.xlsx
```

---

## 🐛 常见问题速查

| 问题 | 解决方法 |
|------|----------|
| 提示找不到模块 | `pip install -r requirements.txt` |
| 找不到反馈列 | `--column 你的列名` |
| CSV中文乱码 | 已支持多编码自动识别，如仍有问题改用`.xlsx` |
| 分析不准确 | 在 `custom_dict.txt` 添加行业词汇 |
| 处理速度慢 | 建议每批1000-2000条 |

---

## 📚 更多信息

- **完整文档**：查看 [README.md](README.md)
- **配置说明**：编辑 `config/config.json`
- **自定义词典**：编辑 `config/custom_dict.txt`
- **查看日志**：打开 `feedback_analysis.log`

---

## 📁 项目文件说明

```
CompetitorFetch/
├── 启动.bat                  ← Windows一键启动（6个选项）
├── START_HERE.md            ← 你在这里！
├── README.md                ← 完整文档
├── feedback_template.xlsx   ← 反馈内容模板
├── request_template.xlsx    ← 请求内容模板 ⭐
├── demo_data.xlsx           ← 演示数据
├── src/                     ← 源代码（7个文件）
├── config/                  ← 配置文件
└── output/                  ← 输出目录（独立文件夹）
```

---

## ✅ 快速检查清单

开始前请确认：

- [ ] 已安装Python 3.10+
- [ ] 已运行 `pip install -r requirements.txt`
- [ ] 数据文件包含反馈文本列
- [ ] 文件格式为 .xlsx / .xls / .csv

---

## 🎉 立即开始

### 第1步：安装依赖
```bash
双击 启动.bat → 选择 [1]
```

### 第2步：查看演示
```bash
# 反馈分析演示
双击 启动.bat → 选择 [2]

# 请求分析演示
双击 启动.bat → 选择 [3]
```

### 第3步：分析自己的数据
```bash
# 反馈分析
python src/main.py your_feedback.xlsx

# 请求分析
python src/main.py your_requests.xlsx --type request
```

---

**🎯 就是这么简单！现在开始分析你的用户反馈吧！**

**遇到问题？** 查看 [README.md](README.md) 或检查 `feedback_analysis.log` 日志文件
